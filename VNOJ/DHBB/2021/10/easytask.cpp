#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0);

    int n, a;
    long long dp;

    cin >> n;
    for (int i = 1; i <= n; i++) {
        cin >> a;
        dp = max(dp, )
    }
}